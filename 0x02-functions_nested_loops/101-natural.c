#include <stdio.h>
#include "main.h"
/**
 * sum_of_multiples - Print the sum of multiples
 *
 * Return: Always 0 (Success)
 */
int main()
{
	int r;
	
	r = sum_of_multiples(1024);

	printf("%d\n", r);
	return (0);
}
